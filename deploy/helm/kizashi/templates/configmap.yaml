apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kizashi.configMapName" . }}
  labels:
    {{- include "kizashi.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.sharedEnv }}
  {{ $key }}: {{ tpl (toString $value) $ | quote }}
  {{- end }}
